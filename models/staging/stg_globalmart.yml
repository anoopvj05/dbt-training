version: 2

models:
 - name: stg_orders
   description: one unique order per row
   columns: 
    - name: orderid
      description: primarky key of stg_orders
    - name: shipmode
      description : '{{doc("shipmode")}}'